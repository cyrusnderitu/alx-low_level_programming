#include "main.h"

/**
 * main - Print alphapets in lowercase with _putchar
 * Return: Always 0 (Success).
 */
int main(void) 
{  
for (int c = 'A'; c < 'Z'; c++)
_putchar(tolower(c))    
return (0);  
}
