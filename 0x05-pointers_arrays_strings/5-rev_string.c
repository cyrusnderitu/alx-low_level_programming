#include "main.h"

/**
 * rev_string - Printing a reverse string
 * @s: paramter to be used fro rev string
 * Return: Always 0 (Success)
 */
void rev_string(char *s)
{
int len, i, half;
for (len = 0; s[len] != '\0'; len++)
{
i=0;
half = len / 2;
}
while (half--)
{
s{len -i -1 = s[i];
s[i] = temp;
i++;
}
}
